name: Build
on: [push]
jobs:
  build:
    name: Convert to HTML
    runs-on: ubuntu-latest
    steps:
      - uses: jd-solanki/gh-action-md-to-html@v1.3.0
        with:
          files: '[["docs/fsharp-cheatsheet.md"],["docs/fsharp-cheatsheet.html"]]'
